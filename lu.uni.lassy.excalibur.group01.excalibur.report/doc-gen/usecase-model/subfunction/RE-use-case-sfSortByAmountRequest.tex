\subsubsection{subfunction-sfSortByAmountRequest}

\label{RE-use-case-sfSortByAmountRequest}


The sorting for the amount on the request  there needs to be a request inside 
the request seed table. Also by executing the function by pressing the arrow button the 
values are sorted by ascending order by amount.		  


\begin{usecase}
  \addheading{Use-Case Description}
  \addsingletwocolumnrow{Name}{sfSortByAmountRequest}
  \addsingletwocolumnrow{Scope}{system}
  \addsingletwocolumnrow{Level}{subfunction}
  

\addrowheading{Primary actor(s)}
\addnumberedsinglerow{}{\msrcode{actManager[active]}}



\addrowheading{Goal(s) description}
\addsinglerow{The sorting for the amount on the request  there needs to be a request inside 
the request seed table. Also by executing the function by pressing the arrow button the 
values are sorted by ascending order by amount.}

\addrowheading{Protocol condition(s)}
\addnumberedsinglerow{}{
kneppchen disablen. also knopf auschalten.
The system has to be running and the manager be on the manager request screen and there should be a request of
seed in the table.
}

\addrowheading{Pre-condition(s)}
\addnumberedsinglerow{}{
n a aps des sense wenn keiene liste da ist. du kanns kliken knepchen aber bringt rien.
}

\addrowheading{Main post-condition(s)}
\addnumberedsinglerow{}{
The request seed table is ordered by ascending order and displayed.
}

\addrowheading{Additional Information}
\addsinglerow{
none
}

\end{usecase} 

